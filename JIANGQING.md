# 江擎 · 水利工程智能协作

此项目从 Yuxi 分支扩展，源码与数据位于 `D:\workspace\jiangqing`。原 `D:\workspace\yuxi` 保留。新服务使用独立 PostgreSQL、Redis、存储目录、Docker 网络及账号。

## 打开与启动

本机入口：`http://127.0.0.1:5174/changwei`。远程设备使用已配置的 Tailscale HTTPS 地址，登录同一个 tailnet；电脑须开机且项目服务运行。当前安装客户端也连接这套后端，不会把 Docker、数据库或模型复制进每台手机。

Docker Desktop 就绪后，在 PowerShell 执行：

```powershell
cd D:\workspace\jiangqing
.\scripts\start-jiangqing.ps1
```

本机已复用现成基础镜像。新机器首次部署需要 `.env`、模型配置、文件/数据库迁移及重新构建镜像，不能只复制此仓库便声称全部可用。需重建时用 `-Build`；构建需要正常网络。检查服务用 `docker compose ps`，检查就绪用 `/api/system/ready`。本地 BGE 采用独立 `compose.local-bge.yml`，权重目录需按实际机器修改，只读挂载且不暴露宿主端口。

## 业务操作

1. 在“工程工作台”新建工程及标段，按需添加已有账号作为成员。
2. 在资料中心上传 DOCX、XLSX、PDF、TXT 或 ZIP，检查内容与位置，再人工确认类别。扫描 PDF 每次可处理五页 OCR；未完成页仍显示待核验。
3. 新建监理日志、项管日志、监理月报、项管月报或施工方案审核任务。给日志填写日期，给月报填写明确报告期，选择本次任务使用的资料。
4. 按模块填写事实，或用模型整理草稿。模型草稿始终需要人工核对；只允许负责人或被分派的成员修改相应模块。
5. 保存与确认分开执行；全部模块确认后由工程负责人生成 Word。历史成果保留，不因后续编辑改变旧文件。
6. 监理日志可选择上传的原版七行八列表格模板，其他成果目前为基础 Word 版式。应检查客户要求的最终版式再正式报送。

手机端提供折叠菜单、任务卡片、横向模块切换和底部固定的语音/保存/确认按钮。主导航为工程工作台、工程总览、工程资料、辅助问答和 AI 中台。工程总览按五类实际任务统计待确认模块、生成状态与资料数量；深度研究等原始演示角色不出现在常用助手列表。知识库和模型配置集中于 AI 中台。

## 已接入与待配置能力

- **文本模型**：复用已有供应商配置；DeepSeek 已通过真实日志整理及数据库回读验证。
- **知识库**：“长委工程参考资料（非规范库）”已索引三份真实参考 DOCX、九个片段，并核对两次检索的原始来源。这些资料不是现行法规规范库。业务模块当前使用所选材料中的关键词窗口，通用知识库向量检索是独立能力，不把两者混称为已集成的完整审查。
- **天气**：配置 `QWEATHER_API_HOST`、`QWEATHER_API_KEY` 于本机 `.env`，后台获取；页面输入明确位置，仅支持北京时间当天日志。结果是带观测时间的实况，不是全天或历史天气，也不会自动确认。
- **实时语音**：已配置北京地域百炼 `qwen3-asr-flash-realtime`，麦克风 PCM 分帧后经 WebSocket 实时转录。2026-09-19 使用约 20 秒合成中文语音，分别经本机接口和 Tailscale HTTPS 完整链路测试；均在约 0.8 秒开始返回中间文字并正常结束。另一次去掉末尾静音的主动停止测试，停止后收到完整末句；三次均未自动修改业务记录。实体手机的麦克风权限、现场噪声和实际口音仍需现场验收。手机需 HTTPS，并明确授予麦克风权限。更换后端 `.env` 中的 `DASHSCOPE_API_KEY` 后执行 `docker compose up -d --no-deps --no-build --force-recreate api worker`，安装客户端无需保存密钥。
- **月报/审查边界**：已支持资料选取、分章节草稿、人工确认、版本成果。复杂台账的当期/累计/单位换算、完整规范有效性与适用性、在线 Word 协同、第三方报送尚未实现，不应将 AI 草稿直接作为正式专业结论。

## 本地配置与数据

`.env`、`work/`、`docker/volumes/` 和模型密钥不提交仓库。安装包也不包含 API Key。登录信息单独保存在本机交付目录，勿转发。

Tailscale Serve 仅供 tailnet 内访问，默认转发本机 5174；Vite 通过 `VITE_ALLOWED_HOSTS` 明确允许对应设备域名，不开放任意 Host。后台本机 API 为 5051，数据库等基础设施不发布宿主端口。

## 验证记录

可执行测试位于 `backend/test/unit/test_changwei_*.py`、`backend/test/integration/check_changwei_speech_live.py` 和 `web/test/unit/changweiTranscription.test.js`。本机真实资料、HTTP、并发、模板、天气、AI、向量检索证据保存在被忽略的 `work/`。执行结果区分本地测试、真实服务和未配置能力，单测不替代真实语音或手机安装验收。
